{
  "name": "demo",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "build:pages": "next build && next-on-pages",
    "deploy:pages": "pnpm run build:pages && cp wrangler.toml .vercel/output/static/ && cd .vercel/output/static && npx wrangler pages deploy . --project-name infinite-canvas-demo",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "@tc/infinite-core": "workspace:*",
    "@tc/infinite-widget": "workspace:*",
    "next": "^15.1.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@cloudflare/next-on-pages": "^1.7.4",
    "@types/node": "^20.11.5",
    "@types/react": "^18.2.48",
    "@types/react-dom": "^18.2.18",
    "typescript": "^5.3.3"
  }
}
